% ============================================================
%  第四章 系统设计
% ============================================================
\chapter{系统设计}

\section{总体架构设计}

\subsection{架构设计原则}

系统架构设计遵循以下核心原则：

\begin{enumerate}
  \item \textbf{模块化原则}：按业务领域划分模块，每个模块职责单一，降低耦合度；
  \item \textbf{分层原则}：采用经典的三层架构（表示层、业务逻辑层、数据访问层），层次清晰；
  \item \textbf{可扩展性原则}：预留扩展点，支持后续功能增加和架构演进；
  \item \textbf{高内聚低耦合原则}：模块内部高度内聚，模块间通过接口交互；
  \item \textbf{安全性原则}：认证授权统一管理，敏感数据加密存储和传输。
\end{enumerate}

\subsection{模块化单体架构}

系统采用”模块化单体”（Modular Monolith）架构方案，这是介于传统单体架构和微服务架构之间的一种折中方案。各业务模块在代码层面解耦，保持清晰的边界，但在部署层面统一打包为单一应用运行。

\subsubsection{模块化单体的优势}

相比传统单体架构，模块化单体具有以下优势：

\begin{enumerate}
  \item \textbf{开发调试成本低}：所有模块在同一进程中运行，无需启动多个独立服务，开发调试更便捷；
  \item \textbf{事务处理简单}：跨模块操作可以直接使用本地事务（ACID），无需分布式事务协调；
  \item \textbf{部署运维简单}：仅需管理单一应用实例，降低了运维复杂度和资源消耗；
  \item \textbf{性能开销小}：模块间调用是进程内方法调用，无网络开销，性能更优；
  \item \textbf{支持渐进式演进}：通过明确的模块边界，后续可以根据需要将特定模块拆分为独立的微服务。
\end{enumerate}

相比微服务架构，模块化单体更适合中小型项目和团队，避免了微服务带来的分布式系统复杂性。

\subsubsection{模块划分策略}

系统按照领域驱动设计（DDD）的思想，将业务划分为以下模块：

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  mod/.style={draw, rectangle, rounded corners, minimum width=2.25cm, minimum height=0.86cm, text width=2.1cm, align=center, font=\small},
  core/.style={draw, rectangle, rounded corners, minimum width=9.8cm, minimum height=0.9cm, align=center, font=\small, fill=blue!12},
  entry/.style={draw, rectangle, rounded corners, minimum width=10.0cm, minimum height=0.9cm, align=center, font=\small\bfseries, fill=orange!18},
  arr/.style={-Stealth, thick}
]
\node[entry] (gw) at (5.5,6.2) {community-gateway（启动入口 / 认证与看板相关控制器）};

\node[mod] (user)     at (1.5,4.4)  {community-user};
\node[mod] (club)     at (4.1,4.4)  {community-club};
\node[mod] (activity) at (6.9,4.4)  {community-activity};
\node[mod] (recruit)  at (9.5,4.4)  {community-recruit};
\node[mod] (notice)   at (4.1,2.6)  {community-notice};
\node[mod] (admin)    at (6.9,2.6)  {community-admin};

\node[core] (core)    at (5.5,0.9)  {community-core（Entity / Repository / 通用组件）};

\coordinate (core1) at ($(core.north west)!0.12!(core.north east)$);
\coordinate (core2) at ($(core.north west)!0.28!(core.north east)$);
\coordinate (core3) at ($(core.north west)!0.44!(core.north east)$);
\coordinate (core4) at ($(core.north west)!0.60!(core.north east)$);
\coordinate (core5) at ($(core.north west)!0.76!(core.north east)$);
\coordinate (core6) at ($(core.north west)!0.90!(core.north east)$);

\draw[arr] (gw.south west) -- ++(-2.8,-0.2) |- (user.north west);
\draw[arr] (gw.south) -- (club.north);
\draw[arr] (gw.south) -- (activity.north);
\draw[arr] (gw.south east) -- ++(2.8,-0.2) |- (recruit.north east);
\draw[arr] (gw.south west) -- ++(-1.4,-0.2) |- (notice.north west);
\draw[arr] (gw.south east) -- ++(1.4,-0.2) |- (admin.north east);

\draw[arr] (user.south) -- (core1);
\draw[arr] (club.south) -- (core2);
\draw[arr] (activity.south) -- (core4);
\draw[arr] (recruit.south) -- (core5);
\draw[arr] (notice.south) -- (core3);
\draw[arr] (admin.south) -- (core6);
\end{tikzpicture}
\caption{后端模块依赖关系图}
\label{fig:module-dep}
\end{figure}

各模块的职责说明：

\begin{itemize}
  \item \textbf{community-core}：核心模块，定义所有 JPA 实体类、Repository 接口、统一返回结构、JWT 工具类、OSS 服务、邮件服务等通用能力。该模块被所有业务模块依赖。

  \item \textbf{community-gateway}：网关模块，作为应用启动入口，包含 Spring Boot 主类、Spring Security 配置、JWT 过滤器、全局异常处理器、WebSocket 配置、Dashboard 统计接口等。

  \item \textbf{community-user}：用户模块，负责用户注册、登录、个人信息管理、密码修改、权限校验服务等。

  \item \textbf{community-club}：社团模块，负责社团的创建、审批、查询、推荐、成员管理、解散流程、资源申请、财务管理、AI 聊天入口等。

  \item \textbf{community-activity}：活动模块，负责活动的创建、查询、报名、签到、签到记录导出等。

  \item \textbf{community-recruit}：招新模块，负责招新批次管理、动态表单字段配置、报名申请、初审终审流程、报名结果导出等。

  \item \textbf{community-notice}：通知模块，负责公告的发布与查询、站内通知的生成与推送、RabbitMQ 消息消费等。

  \item \textbf{community-admin}：管理模块，负责系统管理员的审批功能、审计日志查询、统计数据接口等。
\end{itemize}

模块间的依赖关系遵循"单向依赖"原则：业务模块只依赖 core 模块，业务模块之间不直接依赖，通过消息队列或 API 调用实现跨模块通信。

\subsection{部署与运行拓扑}

系统采用容器化部署方案，所有服务通过 Docker 容器运行，并通过 Docker Compose 进行编排。部署拓扑如图~\ref{fig:deploy} 所示。

\subsubsection{前端层}

前端采用 Vue 3 单页应用，通过 Nginx 提供静态文件服务。用户通过浏览器访问前端页面，前端通过 Nginx 反向代理将 API 请求转发到后端服务。这种架构实现了前后端分离，前端可以独立部署和扩展。

\subsubsection{应用层}

应用层包括两个核心服务：

\begin{enumerate}
  \item \textbf{Spring Boot 应用}：运行在 8080 端口，提供所有业务 API 接口，处理用户请求、业务逻辑、数据持久化等；
  \item \textbf{FastAPI AI 服务}：运行在 8000 端口，独立于主应用部署，提供知识问答和社团推荐功能。
\end{enumerate}

两个服务通过 HTTP REST API 进行通信，实现了业务逻辑与 AI 能力的解耦。

\subsubsection{数据层}

数据层包括多种数据存储和中间件：

\begin{itemize}
  \item \textbf{MySQL 8}：主数据库，存储用户、社团、活动、招新等核心业务数据；
  \item \textbf{Redis 7}：缓存和会话存储，用于 JWT 令牌管理、登录限流等；
  \item \textbf{RabbitMQ 3.12}：消息队列，实现异步通知分发；
  \item \textbf{Aliyun OSS}：对象存储服务，存储用户头像、活动封面等文件；
  \item \textbf{Chroma DB}：向量数据库，存储社团知识库的向量表示，支持语义检索。
\end{itemize}

\subsubsection{监控层}

监控层包括 Prometheus 和 Grafana：

\begin{itemize}
  \item \textbf{Prometheus}：定时抓取 Spring Boot 应用暴露的 \texttt{/actuator/prometheus} 指标端点，收集系统运行指标；
  \item \textbf{Grafana}：连接 Prometheus 数据源，提供可视化监控面板，展示系统性能和业务指标。
\end{itemize}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  box/.style={draw, rectangle, rounded corners, minimum width=2.6cm, minimum height=0.8cm, align=center, font=\small},
  infra/.style={draw, rectangle, rounded corners, minimum width=2.6cm, minimum height=0.8cm, align=center, font=\small, fill=green!14},
  user/.style={draw, rectangle, rounded corners, minimum width=2.6cm, minimum height=0.8cm, align=center, font=\small, fill=yellow!20},
  arr/.style={-Stealth},
  lab/.style={font=\tiny, fill=white, inner sep=1pt}
]
\node[user] (browser) at (0,5.6) {Browser};
\node[box] (nginx) at (3.0,5.6) {Nginx\\frontend:80};
\node[box] (app) at (6.4,5.6) {Spring Boot\\app:8080};
\node[box] (agent) at (10.4,5.6) {FastAPI\\rag-service:8000};

\node[infra] (redis) at (4.0,3.4) {Redis 7};
\node[infra] (mq) at (6.8,3.4) {RabbitMQ 3.12};
\node[infra] (mysql) at (10.4,3.4) {MySQL 8};
\node[infra] (oss) at (13.2,3.4) {Aliyun OSS};

\node[infra] (prom) at (5.2,1.4) {Prometheus};
\node[infra] (grafana) at (7.9,1.4) {Grafana};
\node[infra] (chroma) at (10.4,1.4) {Chroma DB};

\draw[arr] (browser) -- node[above,lab]{HTTP} (nginx);
\draw[arr] (nginx) -- node[above,lab]{/api 反代} (app);
\draw[arr] (app) -- node[above,lab]{REST} (agent);
\draw[arr] (app.south west) -- node[above left,lab]{缓存} (redis.north);
\draw[arr] (app.south) -- node[right,lab]{消息} (mq.north);
\draw[arr] (app.south east) -- node[above,lab]{JPA} (mysql.north west);
\draw[arr] (app.east) -- node[above,lab]{对象存储} (oss.north west);
\draw[arr] (app.south west) -- ++(-0.2,-0.2) |- node[pos=0.8,above,lab]{/actuator/prometheus} (prom.north);
\draw[arr] (prom) -- node[above,lab]{指标看板} (grafana);
\draw[arr] (agent.south) -- node[right,lab]{向量访问} (chroma.north);
\draw[arr] (agent.south west) -- ++(-0.7,-0.15) |- node[pos=0.34,left,lab]{业务数据查询} (mysql.east);
\end{tikzpicture}
\caption{系统部署拓扑图}
\label{fig:deploy}
\end{figure}

\section{数据模型设计}

\subsection{数据库设计原则}

系统数据库设计遵循以下原则：

\begin{enumerate}
  \item \textbf{规范化原则}：数据库设计遵循第三范式（3NF），减少数据冗余，保证数据一致性；
  \item \textbf{性能优化原则}：在关键查询字段上建立索引，提升查询性能；
  \item \textbf{扩展性原则}：预留扩展字段，支持后续功能增加；
  \item \textbf{完整性原则}：通过外键约束、唯一约束、非空约束等保证数据完整性；
  \item \textbf{审计原则}：关键业务表包含创建时间、更新时间、创建人等审计字段。
\end{enumerate}

\subsection{核心实体关系}

系统共设计 19 张数据表，其中 18 张为核心业务表，另有 1 张社团标签辅助表（\texttt{t\_club\_tag}），涵盖用户管理、社团管理、活动管理、招新管理、通知管理、资源管理、财务管理等业务领域。核心实体包括：

\begin{itemize}
  \item \textbf{用户与权限}：\texttt{t\_user}（用户表）、\texttt{t\_role}（角色表）、\texttt{t\_user\_role}（用户角色关联表）
  \item \textbf{社团管理}：\texttt{t\_club}（社团表）、\texttt{t\_member}（成员表）
  \item \textbf{活动管理}：\texttt{t\_activity}（活动表）、\texttt{t\_activity\_signup}（活动报名表）、\texttt{t\_activity\_attendance}（活动签到表）
  \item \textbf{招新管理}：\texttt{t\_recruit\_batch}（招新批次表）、\texttt{t\_recruit\_form\_field}（表单字段表）、\texttt{t\_recruit\_application}（招新申请表）
  \item \textbf{通知管理}：\texttt{t\_notice}（公告表）、\texttt{t\_notice\_read}（公告已读表）、\texttt{t\_notification}（站内通知表）
  \item \textbf{资源管理}：\texttt{t\_resource}（资源定义表）、\texttt{t\_resource\_application}（资源申请表）
  \item \textbf{财务管理}：\texttt{t\_club\_finance}（财务流水表）
  \item \textbf{审计管理}：\texttt{t\_audit\_log}（审计日志表）
\end{itemize}

实体间的关系遵循以下设计：

\begin{itemize}
  \item 用户与角色是多对多关系，通过中间表 \texttt{t\_user\_role} 关联；
  \item 用户与社团是多对多关系，通过成员表 \texttt{t\_member} 关联，成员表记录用户在社团中的角色；
  \item 社团与活动、招新批次、公告、资源申请、财务流水是一对多关系；
  \item 活动与报名、签到是一对多关系；
  \item 招新批次与表单字段、招新申请是一对多关系。
\end{itemize}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  entity/.style={draw, rectangle, minimum width=2.2cm, minimum height=0.72cm, align=center, font=\small\bfseries, fill=blue!10},
  line/.style={-Stealth, thin},
  rel/.style={font=\tiny, fill=white, inner sep=1pt}
]
\node[entity] (role) at (0,8.2) {t\_role};
\node[entity] (user) at (0,6.4) {t\_user};
\node[entity] (notification) at (0,2.8) {t\_notification};

\node[entity] (member) at (3.6,6.4) {t\_member};
\node[entity] (app) at (3.6,4.6) {t\_recruit\_application};
\node[entity] (notice) at (3.6,2.8) {t\_notice};

\node[entity] (club) at (7.2,6.4) {t\_club};
\node[entity] (batch) at (7.2,4.6) {t\_recruit\_batch};

\node[entity] (activity) at (10.8,6.4) {t\_activity};
\node[entity] (attendance) at (10.8,4.6) {t\_activity\_attendance};
\node[entity] (finance) at (10.8,1.0) {t\_club\_finance};
\node[entity] (resource) at (10.8,2.8) {t\_resource};

\node[entity] (signup) at (14.4,6.4) {t\_activity\_signup};
\node[entity] (resourceApp) at (14.4,2.8) {t\_resource\_application};

\draw[line] (role) -- node[left,rel]{1:N} (user);
\draw[line] (user.east) -- node[above,rel]{1:N} (member.west);
\draw[line] (club.west) -- node[above,rel]{1:N} (member.east);
\draw[line] (club.south) -- node[right,rel]{1:N} (batch.north);
\draw[line] (batch.west) -- node[above,rel]{1:N} (app.east);
\draw[line] (user.south east) -- ++(0.7,-0.2) |- node[pos=0.74,right,rel]{1:N} (app.north west);
\draw[line] (user.south) -- node[left,rel]{1:N} (notification.north);
\draw[line] (club.south west) -- ++(-0.7,-0.4) |- node[pos=0.72,above,rel]{1:N} (notice.north);
\draw[line] (club.south east) -- ++(0.8,-0.4) |- node[pos=0.74,above,rel]{1:N} (finance.north);
\draw[line] (club.east) -- node[above,rel]{1:N} (activity.west);
\draw[line] (activity.south) -- node[right,rel]{1:N} (attendance.north);
\draw[line] (activity.east) -- node[above,rel]{1:N} (signup.west);
\draw[line] (resource.east) -- node[above,rel]{1:N} (resourceApp.west);
\draw[line] (club.east) -- ++(0.9,0) |- node[pos=0.76,right,rel]{1:N} (resourceApp.north west);
\draw[line] (user.north east) -- ++(0.5,0.9) -| node[pos=0.78,above,rel]{1:N} (attendance.north west);
\end{tikzpicture}
\caption{核心实体关系图}
\label{fig:er}
\end{figure}

\subsection{关键表结构说明}

系统共设计 19 张数据表，其中 18 张为核心业务表，以下主要说明各核心业务表的结构设计。

\subsubsection{用户表（t\_user）}

用户表存储系统所有用户的基本信息和认证凭据。

\begin{table}[H]
\centering
\caption{用户表结构}
\begin{tabular}{llll}
\toprule
字段名 & 数据类型 & 约束 & 说明 \\
\midrule
id & BIGINT & PRIMARY KEY & 用户ID \\
username & VARCHAR(50) & UNIQUE, NOT NULL & 用户名 \\
password\_hash & VARCHAR(255) & NOT NULL & 密码哈希值 \\
real\_name & VARCHAR(50) & & 真实姓名 \\
email & VARCHAR(100) & UNIQUE & 邮箱地址 \\
mobile & VARCHAR(20) & & 手机号码 \\
avatar\_url & VARCHAR(500) & & 头像URL \\
interests & TEXT & & 兴趣标签（JSON） \\
status & VARCHAR(20) & NOT NULL & 账号状态 \\
created\_at & DATETIME & NOT NULL & 创建时间 \\
updated\_at & DATETIME & NOT NULL & 更新时间 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{角色表（t\_role）}

角色表定义系统的全局角色，包括普通用户、社团管理员、系统管理员等。

\begin{table}[H]
\centering
\caption{角色表结构}
\begin{tabular}{llll}
\toprule
字段名 & 数据类型 & 约束 & 说明 \\
\midrule
id & BIGINT & PRIMARY KEY & 角色ID \\
code & VARCHAR(50) & UNIQUE, NOT NULL & 角色编码 \\
name & VARCHAR(50) & NOT NULL & 角色名称 \\
description & VARCHAR(200) & & 角色描述 \\
created\_at & DATETIME & NOT NULL & 创建时间 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{用户角色关联表（t\_user\_role）}

用户角色关联表维护用户与角色的多对多关系。

\begin{table}[H]
\centering
\caption{用户角色关联表结构}
\begin{tabular}{llll}
\toprule
字段名 & 数据类型 & 约束 & 说明 \\
\midrule
user\_id & BIGINT & FOREIGN KEY, NOT NULL & 用户ID \\
role\_id & BIGINT & FOREIGN KEY, NOT NULL & 角色ID \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{社团表（t\_club）}

社团表存储社团的基本信息、状态和财务数据。

\begin{table}[H]
\centering
\caption{社团表结构}
\begin{tabular}{llll}
\toprule
字段名 & 数据类型 & 约束 & 说明 \\
\midrule
id & BIGINT & PRIMARY KEY & 社团ID \\
name & VARCHAR(100) & UNIQUE, NOT NULL & 社团名称 \\
short\_name & VARCHAR(50) & UNIQUE & 社团简称 \\
category & VARCHAR(50) & NOT NULL & 社团类别 \\
description & TEXT & & 社团简介 \\
status & VARCHAR(20) & NOT NULL & 社团状态 \\
created\_by & BIGINT & FOREIGN KEY & 创建人ID \\
balance & DECIMAL(10,2) & DEFAULT 0 & 账户余额 \\
created\_at & DATETIME & NOT NULL & 创建时间 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{活动表（t\_activity）}

活动表存储社团活动的基本信息和签到配置。

\begin{table}[H]
\centering
\caption{活动表结构}
\begin{tabular}{llll}
\toprule
字段名 & 数据类型 & 约束 & 说明 \\
\midrule
id & BIGINT & PRIMARY KEY & 活动ID \\
club\_id & BIGINT & FOREIGN KEY, NOT NULL & 社团ID \\
title & VARCHAR(200) & NOT NULL & 活动标题 \\
description & TEXT & & 活动描述 \\
location & VARCHAR(200) & & 活动地点 \\
start\_time & DATETIME & NOT NULL & 开始时间 \\
end\_time & DATETIME & NOT NULL & 结束时间 \\
checkin\_code & VARCHAR(20) & & 签到码 \\
status & VARCHAR(20) & NOT NULL & 活动状态 \\
created\_at & DATETIME & NOT NULL & 创建时间 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{活动报名表（t\_activity\_signup）}

活动报名表记录用户对活动的报名信息。

\begin{table}[H]
\centering
\caption{活动报名表结构}
\begin{tabular}{llll}
\toprule
字段名 & 数据类型 & 约束 & 说明 \\
\midrule
id & BIGINT & PRIMARY KEY & 报名ID \\
activity\_id & BIGINT & FOREIGN KEY, NOT NULL & 活动ID \\
user\_id & BIGINT & FOREIGN KEY, NOT NULL & 用户ID \\
status & VARCHAR(20) & NOT NULL & 报名状态 \\
signup\_time & DATETIME & NOT NULL & 报名时间 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{活动签到表（t\_activity\_attendance）}

活动签到表记录用户的活动签到信息。

\begin{table}[H]
\centering
\caption{活动签到表结构}
\begin{tabular}{llll}
\toprule
字段名 & 数据类型 & 约束 & 说明 \\
\midrule
id & BIGINT & PRIMARY KEY & 签到ID \\
activity\_id & BIGINT & FOREIGN KEY, NOT NULL & 活动ID \\
user\_id & BIGINT & FOREIGN KEY, NOT NULL & 用户ID \\
sign\_time & DATETIME & NOT NULL & 签到时间 \\
source & VARCHAR(20) & & 签到来源 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{招新批次表（t\_recruit\_batch）}

招新批次表定义招新活动的基本信息和时间范围。

\begin{table}[H]
\centering
\caption{招新批次表结构}
\begin{tabular}{llll}
\toprule
字段名 & 数据类型 & 约束 & 说明 \\
\midrule
id & BIGINT & PRIMARY KEY & 批次ID \\
club\_id & BIGINT & FOREIGN KEY, NOT NULL & 社团ID \\
title & VARCHAR(200) & NOT NULL & 批次标题 \\
description & TEXT & & 批次描述 \\
start\_time & DATETIME & NOT NULL & 开始时间 \\
end\_time & DATETIME & NOT NULL & 结束时间 \\
quota & INT & NOT NULL & 招新名额 \\
status & VARCHAR(20) & NOT NULL & 批次状态 \\
created\_at & DATETIME & NOT NULL & 创建时间 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{招新表单字段表（t\_recruit\_form\_field）}

招新表单字段表定义招新批次的动态表单字段配置。

\begin{table}[H]
\centering
\caption{招新表单字段表结构}
\begin{tabular}{llll}
\toprule
字段名 & 数据类型 & 约束 & 说明 \\
\midrule
id & BIGINT & PRIMARY KEY & 字段ID \\
batch\_id & BIGINT & FOREIGN KEY, NOT NULL & 批次ID \\
field\_name & VARCHAR(50) & NOT NULL & 字段名称 \\
field\_label & VARCHAR(100) & NOT NULL & 字段标签 \\
field\_type & VARCHAR(20) & NOT NULL & 字段类型 \\
required & BOOLEAN & NOT NULL & 是否必填 \\
options & TEXT & & 选项列表 \\
sort\_order & INT & & 排序顺序 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{招新申请表（t\_recruit\_application）}

招新申请表记录学生的报名申请和审核状态。

\begin{table}[H]
\centering
\caption{招新申请表结构}
\begin{tabular}{llll}
\toprule
字段名 & 数据类型 & 约束 & 说明 \\
\midrule
id & BIGINT & PRIMARY KEY & 申请ID \\
batch\_id & BIGINT & FOREIGN KEY, NOT NULL & 批次ID \\
user\_id & BIGINT & FOREIGN KEY, NOT NULL & 用户ID \\
form\_data & TEXT & & 表单数据（JSON） \\
first\_review\_status & VARCHAR(20) & & 初审状态 \\
first\_review\_comment & TEXT & & 初审意见 \\
final\_review\_status & VARCHAR(20) & & 终审状态 \\
final\_review\_comment & TEXT & & 终审意见 \\
created\_at & DATETIME & NOT NULL & 创建时间 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{成员表（t\_member）}

成员表记录用户在社团中的成员关系和角色。

\begin{table}[H]
\centering
\caption{成员表结构}
\begin{tabular}{llll}
\toprule
字段名 & 数据类型 & 约束 & 说明 \\
\midrule
id & BIGINT & PRIMARY KEY & 成员ID \\
club\_id & BIGINT & FOREIGN KEY, NOT NULL & 社团ID \\
user\_id & BIGINT & FOREIGN KEY, NOT NULL & 用户ID \\
role\_code & VARCHAR(20) & NOT NULL & 角色代码 \\
status & VARCHAR(20) & NOT NULL & 成员状态 \\
join\_at & DATETIME & NOT NULL & 加入时间 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{公告表（t\_notice）}

公告表同时存储系统公告与社团公告信息；当公告属于系统级发布时，\texttt{club\_id} 可以为空。

\begin{table}[H]
\centering
\caption{公告表结构}
\begin{tabular}{llll}
\toprule
字段名 & 数据类型 & 约束 & 说明 \\
\midrule
id & BIGINT & PRIMARY KEY & 公告ID \\
club\_id & BIGINT & FOREIGN KEY, NULLABLE & 社团ID，系统公告可为空 \\
title & VARCHAR(200) & NOT NULL & 公告标题 \\
content & TEXT & NOT NULL & 公告内容 \\
scope & VARCHAR(20) & NOT NULL & 公告范围（如 \texttt{PUBLIC}） \\
published\_by & BIGINT & FOREIGN KEY, NOT NULL & 发布人ID \\
published\_at & DATETIME & & 发布时间 \\
status & VARCHAR(20) & NOT NULL & 公告状态 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{公告已读表（t\_notice\_read）}

公告已读表记录用户对公告的阅读状态。

\begin{table}[H]
\centering
\caption{公告已读表结构}
\begin{tabular}{llll}
\toprule
字段名 & 数据类型 & 约束 & 说明 \\
\midrule
id & BIGINT & PRIMARY KEY & 记录ID \\
notice\_id & BIGINT & FOREIGN KEY, NOT NULL & 公告ID \\
user\_id & BIGINT & FOREIGN KEY, NOT NULL & 用户ID \\
read\_at & DATETIME & NOT NULL & 阅读时间 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{站内通知表（t\_notification）}

站内通知表记录发送给用户的系统通知消息。

\begin{table}[H]
\centering
\caption{站内通知表结构}
\begin{tabular}{llll}
\toprule
字段名 & 数据类型 & 约束 & 说明 \\
\midrule
id & BIGINT & PRIMARY KEY & 通知ID \\
user\_id & BIGINT & FOREIGN KEY, NOT NULL & 用户ID \\
title & VARCHAR(200) & NOT NULL & 通知标题 \\
content & TEXT & NOT NULL & 通知内容 \\
type & VARCHAR(50) & NOT NULL & 通知类型 \\
read\_flag & BOOLEAN & NOT NULL & 已读标记 \\
created\_at & DATETIME & NOT NULL & 创建时间 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{资源定义表（t\_resource）}

资源定义表存储可申请的场地和物资资源信息。

\begin{table}[H]
\centering
\caption{资源定义表结构}
\begin{tabular}{llll}
\toprule
字段名 & 数据类型 & 约束 & 说明 \\
\midrule
id & BIGINT & PRIMARY KEY & 资源ID \\
name & VARCHAR(100) & NOT NULL & 资源名称 \\
type & VARCHAR(20) & NOT NULL & 资源类型 \\
description & TEXT & & 资源描述 \\
total\_quantity & INT & & 总数量 \\
status & VARCHAR(20) & NOT NULL & 资源状态 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{资源申请表（t\_resource\_application）}

资源申请表记录社团对资源的申请和审批流程。

\begin{table}[H]
\centering
\caption{资源申请表结构}
\begin{tabular}{llll}
\toprule
字段名 & 数据类型 & 约束 & 说明 \\
\midrule
id & BIGINT & PRIMARY KEY & 申请ID \\
club\_id & BIGINT & FOREIGN KEY, NOT NULL & 社团ID \\
resource\_id & BIGINT & FOREIGN KEY, NOT NULL & 资源ID \\
start\_time & DATETIME & NOT NULL & 开始时间 \\
end\_time & DATETIME & NOT NULL & 结束时间 \\
quantity & INT & & 申请数量 \\
status & VARCHAR(20) & NOT NULL & 申请状态 \\
applicant\_id & BIGINT & FOREIGN KEY & 申请人ID \\
approver\_id & BIGINT & FOREIGN KEY & 审批人ID \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{财务流水表（t\_club\_finance）}

财务流水表记录社团的收入和支出流水。

\begin{table}[H]
\centering
\caption{财务流水表结构}
\begin{tabular}{llll}
\toprule
字段名 & 数据类型 & 约束 & 说明 \\
\midrule
id & BIGINT & PRIMARY KEY & 流水ID \\
club\_id & BIGINT & FOREIGN KEY, NOT NULL & 社团ID \\
type & VARCHAR(20) & NOT NULL & 类型 \\
amount & DECIMAL(10,2) & NOT NULL & 金额 \\
description & TEXT & & 描述 \\
status & VARCHAR(20) & NOT NULL & 状态 \\
applicant\_id & BIGINT & FOREIGN KEY & 申请人ID \\
approver\_id & BIGINT & FOREIGN KEY & 审批人ID \\
created\_at & DATETIME & NOT NULL & 创建时间 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{审计日志表（t\_audit\_log）}

审计日志表记录系统关键操作的审计信息。

\begin{table}[H]
\centering
\caption{审计日志表结构}
\begin{tabular}{llll}
\toprule
字段名 & 数据类型 & 约束 & 说明 \\
\midrule
id & BIGINT & PRIMARY KEY & 日志ID \\
user\_id & BIGINT & FOREIGN KEY & 用户ID \\
action & VARCHAR(100) & NOT NULL & 操作动作 \\
resource\_type & VARCHAR(50) & & 资源类型 \\
resource\_id & VARCHAR(50) & & 资源ID \\
detail & TEXT & & 详细信息 \\
ip & VARCHAR(50) & & IP地址 \\
created\_at & DATETIME & NOT NULL & 创建时间 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{索引与并发控制设计}

数据库索引与锁策略围绕高频查询和并发写冲突场景设计：
\begin{enumerate}
  \item 索引：如 \texttt{idx\_activity\_club}、\texttt{idx\_activity\_status}、\texttt{idx\_audit\_created\_at} 等；
  \item 唯一约束：如活动报名唯一键 \texttt{(activity\_id, user\_id)}、招新申请唯一键 \texttt{(batch\_id, user\_id)}；
  \item 悲观锁：在活动报名、签到、终审录取、资源审批、财务审批等关键路径使用 \texttt{for update} 语义，保障幂等与一致性。
\end{enumerate}

\section{安全与权限设计}

\subsection{请求入口权限策略}

网关安全配置采用“公开端点 + 认证端点 + 角色端点”三层策略，典型规则如下：
\begin{enumerate}
  \item \texttt{/api/auth/**}、公开查询类 GET 接口放行；
  \item \texttt{/api/clubs/my} 等用户私有接口要求登录；
  \item \texttt{/actuator/**} 仅 \texttt{ADMIN} 可访问；
  \item 其他接口默认需要认证。
\end{enumerate}

\subsection{认证时序设计}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  actor/.style={font=\small},
  msg/.style={-Stealth, thin},
  ret/.style={-Stealth, thin, dashed},
  lab/.style={font=\tiny, fill=white, inner sep=1pt}
]
\draw[thick] (0,8) -- (0,0);  \node[actor] at (0,8.35) {浏览器};
\draw[thick] (3.6,8) -- (3.6,0); \node[actor] at (3.6,8.35) {AuthController};
\draw[thick] (7.2,8) -- (7.2,0); \node[actor] at (7.2,8.35) {Redis};
\draw[thick] (10.8,8) -- (10.8,0); \node[actor] at (10.8,8.35) {UserDetailsService/DB};

\draw[msg] (0,7.4) -- node[above,lab]{POST /api/auth/login} (3.6,7.4);
\draw[msg] (3.6,6.9) -- node[above,lab]{认证用户名密码} (10.8,6.9);
\draw[ret] (10.8,6.6) -- (3.6,6.6);
\draw[msg] (3.6,6.1) -- node[above,lab]{写入 auth:token:* 与 auth:refresh:*} (7.2,6.1);
\draw[ret] (3.6,5.6) -- node[above,lab]{Set-Cookie: access\_token, refresh\_token} (0,5.6);

\draw[msg] (0,4.7) -- node[above,lab]{业务请求（携带 Cookie）} (3.6,4.7);
\draw[msg] (3.6,4.2) -- node[above,lab]{JWT 验签 + Redis 会话校验} (7.2,4.2);
\draw[ret] (7.2,3.9) -- (3.6,3.9);
\draw[ret] (3.6,3.4) -- node[above,lab]{放行业务接口} (0,3.4);

\draw[msg] (0,2.5) -- node[above,lab]{POST /api/auth/refresh} (3.6,2.5);
\draw[msg] (3.6,2.0) -- node[above,lab]{校验并轮换 refresh token} (7.2,2.0);
\draw[ret] (3.6,1.5) -- node[above,lab]{下发新 access\_token Cookie} (0,1.5);
\end{tikzpicture}
\caption{认证与会话续期时序图}
\label{fig:auth-seq}
\end{figure}

\subsection{授权模型设计}

系统采用“Spring Security 角色授权 + 业务权限服务”组合模型\cite{rbac_model}：
\begin{enumerate}
  \item 方法注解层：\texttt{@PreAuthorize} 用于少量全局角色限制；
  \item 业务规则层：\texttt{PermissionService} 校验是否为系统管理员、是否为社团管理员（\texttt{PRESIDENT/MANAGER}）以及社团状态是否允许操作；
  \item 数据权限层：多数写操作要求“当前用户与目标社团关联且角色满足条件”。
\end{enumerate}

\begin{table}[H]
\centering
\caption{典型接口权限矩阵}
\label{tab:rbac}
\begin{tabular}{lccc}
\toprule
接口 & USER & CLUB\_ADMIN & ADMIN \\
\midrule
GET /api/clubs, /api/activities, /api/notices & \checkmark & \checkmark & \checkmark \\
POST /api/clubs & \checkmark & \checkmark & \checkmark \\
POST /api/activities &  & \checkmark & \checkmark \\
POST /api/recruit/applications/\{id\}/final-review &  & \checkmark & \checkmark \\
POST /api/resources/applications/\{id\}/approve &  &  & \checkmark \\
GET /api/admin/audit-logs &  &  & \checkmark \\
\bottomrule
\end{tabular}
\end{table}

\section{接口与前端交互设计}

\subsection{统一响应约定}

后端统一使用如下响应结构，便于前端拦截器进行标准化处理：
\begin{lstlisting}[language=Java, caption=统一响应结构]
{
  "code": 200,
  "message": "Success",
  "data": { ... }
}
\end{lstlisting}

\subsection{前端路由与会话联动}

前端路由守卫在访问受限路由前读取 Pinia 中认证状态，并按需调用 \texttt{/api/users/me} 同步最新角色信息；当后端返回 401/403 时，Axios 拦截器统一触发登出与重定向，保证会话状态一致性。

\subsection{活动报名与签到交互时序}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  actor/.style={font=\small},
  msg/.style={-Stealth, thin},
  ret/.style={-Stealth, thin, dashed},
  lab/.style={font=\tiny, fill=white, inner sep=1pt}
]
\draw[thick] (0,9.2) -- (0,0);   \node[actor] at (0,9.55) {浏览器};
\draw[thick] (3.3,9.2) -- (3.3,0); \node[actor] at (3.3,9.55) {ActivityController};
\draw[thick] (6.6,9.2) -- (6.6,0); \node[actor] at (6.6,9.55) {ActivityService};
\draw[thick] (9.9,9.2) -- (9.9,0); \node[actor] at (9.9,9.55) {MySQL};
\draw[thick] (13.2,9.2) -- (13.2,0); \node[actor] at (13.2,9.55) {WebSocket Broker};
\draw[thick] (16.5,9.2) -- (16.5,0); \node[actor] at (16.5,9.55) {管理端看板};

\draw[msg] (0,8.6) -- node[above,lab]{POST /api/activities/\{id\}/signup} (3.3,8.6);
\draw[msg] (3.3,8.1) -- node[above,lab]{校验角色与参数} (6.6,8.1);
\draw[msg] (6.6,7.6) -- node[above,lab]{查询活动、报名记录、剩余名额} (9.9,7.6);
\draw[ret] (9.9,7.3) -- (6.6,7.3);
\draw[msg] (6.6,6.8) -- node[above,lab]{写入报名记录/更新状态} (9.9,6.8);
\draw[ret] (6.6,6.3) -- node[above,lab]{返回报名成功} (0,6.3);

\draw[msg] (0,5.2) -- node[above,lab]{POST /api/activities/\{id\}/checkin} (3.3,5.2);
\draw[msg] (3.3,4.7) -- node[above,lab]{转发签到请求} (6.6,4.7);
\draw[msg] (6.6,4.2) -- node[above,lab]{校验报名状态、时间窗、签到码} (9.9,4.2);
\draw[ret] (9.9,3.9) -- (6.6,3.9);
\draw[msg] (6.6,3.4) -- node[above,lab]{写入签到记录并统计人数} (9.9,3.4);
\draw[msg] (6.6,2.7) -- node[above,lab]{推送 /topic/activity/\{id\}/checkin} (13.2,2.7);
\draw[msg] (13.2,2.2) -- node[above,lab]{广播最新签到人数} (16.5,2.2);
\draw[ret] (6.6,1.5) -- node[above,lab]{HTTP 200 / 业务错误} (0,1.5);
\end{tikzpicture}
\caption{活动报名与签到交互时序图}
\label{fig:activity-seq}
\end{figure}

图~\ref{fig:activity-seq} 体现了活动模块中“同步写入 + 实时推送”的组合设计：报名与签到请求均通过 HTTP 进入主业务链路，签到成功后再额外触发 WebSocket 推送，使页面侧无需轮询即可感知人数变化。这种时序安排也与第五章中的实现代码和第六章中的链路验证保持一致。

\section{AI 服务设计}

\subsection{问答链路}

问答模块由 Java 后端 \texttt{/api/club/chat} 转发至 Python \texttt{/chat}。Python 侧通过 LangChain Agent 调用检索与业务工具，优先返回检索增强结果；若检索不可用则回退至降级回答。

\subsection{推荐链路}

推荐模块由 Java 侧调用 Python \texttt{/recommend}。算法融合协同过滤与语义相似度，若用户无历史行为或推理异常，则回退热门社团列表，保障接口可用性和结果可解释性。

\subsection{AI 服务架构设计}

AI 服务采用独立部署的微服务架构，与主应用解耦，具有以下设计特点：

\begin{enumerate}
  \item \textbf{服务隔离}：AI 服务独立运行在 8000 端口，与主应用（8080 端口）物理隔离，避免 AI 推理的高计算负载影响主业务响应性能；

  \item \textbf{独立服务调用}：主应用通过 HTTP REST 接口调用 AI 服务。当前版本采用同步请求-响应模式，便于与既有业务接口集成，同时保持 AI 链路与主业务链路的进程隔离；

  \item \textbf{降级策略}：当 AI 服务不可用、向量检索失败或推荐推理异常时，系统分别返回错误提示、谨慎回答或热门社团回退结果，保证核心业务不受影响；

  \item \textbf{独立演进}：由于 AI 服务与主应用解耦，后续可在不改动主要业务模块的前提下调整模型、提示词、检索策略与推荐权重；

  \item \textbf{边界清晰}：问答、推荐与知识库维护均集中在 Python 服务侧，便于单独测试、补测与留档。
\end{enumerate}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  box/.style={draw, rectangle, rounded corners, minimum width=2.4cm, minimum height=0.8cm, align=center, font=\small},
  arr/.style={-Stealth, thick},
  lab/.style={font=\tiny, fill=white, inner sep=1pt}
]
\node[box, fill=blue!15] (app) at (0,3) {Spring Boot\\主应用};
\node[box, fill=green!15] (ai) at (5,3) {FastAPI\\AI 服务};
\node[box, fill=orange!15] (llm) at (5,0.8) {DeepSeek\\大模型};
\node[box, fill=purple!15] (chroma) at (10.0,3) {Chroma\\向量库};
\node[box, fill=yellow!15] (mysql) at (0,0.8) {MySQL\\业务数据};

\draw[arr] (app) -- node[above,lab]{HTTP REST} (ai);
\draw[arr] (app.south) -- node[left,lab]{业务查询} (mysql.north);
\draw[arr] (ai.south) -- node[right,lab]{API 调用} (llm.north);
\draw[arr] (ai) -- node[above,lab]{向量检索} (chroma);
\draw[arr] (ai.south west) -- ++(-0.8,0) |- node[pos=0.32,below,lab]{业务数据访问} (mysql.east);
\end{tikzpicture}
\caption{AI 服务架构图}
\label{fig:ai-arch}
\end{figure}

\subsection{知识库维护机制}

AI 问答服务依赖知识库的可维护性。当前版本采用“启动时初始化 + 手动补充”的方式维护向量库：

\begin{enumerate}
  \item \textbf{启动初始化}：AI 服务启动时从 \texttt{agent/data/} 目录加载 Markdown 知识文件，并构建本地 Chroma 向量库；

  \item \textbf{手动补充}：通过 \texttt{/sync} 接口接收新增文本内容与来源标记，将补充材料写入现有向量库，适用于阶段性演示、答辩或小规模内容更新；

  \item \textbf{文档切分}：文本使用 \texttt{RecursiveCharacterTextSplitter} 切分，当前参数为 \texttt{chunk\_size=1000}、\texttt{chunk\_overlap=200}，以平衡检索粒度与上下文完整性；

  \item \textbf{向量化}：嵌入层通过本地封装函数创建，默认使用 \texttt{all-MiniLM-L6-v2}；若句向量依赖不可用，则回退为本地哈希向量表示，以保证离线环境下的基本可运行性；

  \item \textbf{持久化}：向量索引保存在 \texttt{agent/chroma\_db/}，便于本地复现与重复加载。
\end{enumerate}

\section{性能优化设计}

\subsection{数据库查询优化}

系统在数据库层面采用多种优化策略，提升查询性能：

\begin{enumerate}
  \item \textbf{索引优化}：在高频查询字段上建立索引，如社团状态（\texttt{idx\_club\_status}）、活动时间（\texttt{idx\_activity\_start\_time}）、审计日志创建时间（\texttt{idx\_audit\_created\_at}）等；

  \item \textbf{复合索引}：对于多字段联合查询，建立复合索引，如活动报名表的 \texttt{(activity\_id, user\_id)} 复合唯一索引，既保证唯一性又加速查询；

  \item \textbf{分页查询}：所有列表查询接口均采用分页机制，默认每页 20 条记录，避免一次性加载大量数据；

  \item \textbf{延迟加载}：JPA 实体关联关系默认使用 \texttt{LAZY} 加载策略，仅在实际访问关联对象时才触发查询，减少不必要的 JOIN 操作；

  \item \textbf{查询优化}：避免 N+1 查询问题，使用 \texttt{JOIN FETCH} 或 \texttt{@EntityGraph} 一次性加载关联数据。
\end{enumerate}



\subsection{缓存策略设计}

系统使用 Redis 作为分布式缓存，缓存策略如下：

\begin{enumerate}
  \item \textbf{会话缓存}：用户登录后的 JWT 令牌存储在 Redis 中，键名为 \texttt{auth:token:\{jwt\}}，过期时间与令牌一致（1 小时），支持主动失效；

  \item \textbf{限流计数}：登录失败次数、聊天请求频率等限流数据存储在 Redis 中，利用 Redis 的原子递增操作和过期机制实现滑动窗口限流；

  \item \textbf{热点数据缓存}：社团列表、活动列表等高频查询数据可以缓存在 Redis 中，设置合理的过期时间（如 5 分钟），减少数据库压力；

  \item \textbf{缓存更新策略}：采用"先更新数据库，再删除缓存"的策略，保证数据一致性，下次查询时自动重建缓存；

  \item \textbf{缓存穿透防护}：对于不存在的数据，缓存空值（如空列表），设置较短的过期时间（如 1 分钟），避免恶意查询击穿缓存。
\end{enumerate}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  box/.style={draw, rectangle, rounded corners, minimum width=2.5cm, minimum height=0.8cm, align=center, font=\small},
  arr/.style={-Stealth, thick},
  lab/.style={font=\tiny, fill=white, inner sep=1pt}
]
\node[box, fill=blue!15] (browser) at (0,2.8) {浏览器};
\node[box, fill=green!15] (app) at (3.6,2.8) {Spring Boot\\业务接口};
\node[box, fill=orange!20] (redis) at (7.2,2.8) {Redis\\热点缓存};
\node[box, fill=yellow!20] (mysql) at (10.8,2.8) {MySQL\\业务数据};
\node[box, fill=purple!15] (write) at (3.6,0.6) {写操作\\创建/审批/修改};
\node[box, fill=gray!15] (invalidate) at (7.2,0.6) {删除相关缓存键\\等待下次重建};

\draw[arr] (browser) -- node[above,lab]{GET 社团/活动列表} (app);
\draw[arr] (app.north east) -- ++(0,0.45) -| node[pos=0.66,above,lab]{读取缓存} (redis.north west);
\draw[arr] (redis.south west) -- ++(0,-0.45) -| node[pos=0.34,below,lab]{命中直接返回} (app.south east);
\draw[arr] (redis.east) -- ++(0.45,0) |- node[pos=0.22,right,lab]{未命中} (mysql.north);
\draw[arr] (mysql.south west) -- ++(-0.6,-0.45) -| node[pos=0.36,below,lab]{查询结果} (app.south east);
\draw[arr] (app.south) -- node[left,lab]{写请求} (write.north);
\draw[arr] (write.south east) -- ++(0.3,-0.35) -| node[pos=0.68,below,lab]{更新数据库} (mysql.south west);
\draw[arr] (write) -- node[above,lab]{删除缓存} (invalidate);
\draw[arr] (invalidate.north) -- ++(0,0.45) -| node[pos=0.76,above,lab]{下一次读取重建缓存} (redis.south);
\end{tikzpicture}
\caption{热点数据缓存读写与失效流程图}
\label{fig:cache-flow}
\end{figure}

图~\ref{fig:cache-flow} 展示了当前版本采用的“读缓存优先、写后删缓存”策略。对于社团列表、活动列表等高频查询，读请求优先命中 Redis；写请求先落库，再删除相关缓存键，由后续首次读取完成缓存重建，以在实现复杂度与一致性之间取得平衡。

\subsection{并发控制设计}

系统在高并发场景下采用多种并发控制机制，保证数据一致性：

\begin{enumerate}
  \item \textbf{悲观锁}：在招新终审、活动报名、资源审批、财务审批等关键业务场景使用数据库悲观锁（\texttt{SELECT ... FOR UPDATE}），确保同一时刻只有一个事务可以修改数据；

  \item \textbf{唯一约束}：在数据库层面建立唯一约束，如活动报名表的 \texttt{(activity\_id, user\_id)} 唯一键，从根本上防止重复数据；

  \item \textbf{重复写入防护}：关键写接口以“避免重复写入和状态错乱”为目标，主要依赖唯一约束、业务状态校验与悲观锁实现；在当前版本中，重复报名、重复签到、重复审批等请求通常返回业务错误，而不是一律以“幂等成功”方式吞掉重复调用；

  \item \textbf{分布式锁}：对于跨实例的并发控制场景（如定时任务），可以使用 Redis 分布式锁，保证同一时刻只有一个实例执行任务；

  \item \textbf{乐观锁}：对于冲突概率较低的场景，可以使用版本号机制实现乐观锁，减少锁等待时间。
\end{enumerate}


\section{异常处理与日志设计}

\subsection{统一异常处理机制}

系统通过 Spring 的全局异常处理器统一处理各类异常，保证错误响应的一致性：

\begin{enumerate}
  \item \textbf{业务异常}：自定义 \texttt{BusinessException} 类，包含错误码和错误消息，用于表示可预期的业务错误（如"名额已满"、"权限不足"等）；

  \item \textbf{参数校验异常}：使用 JSR-303 Bean Validation 进行参数校验，校验失败时抛出 \texttt{MethodArgumentNotValidException}，全局处理器提取校验错误信息并返回；

  \item \textbf{认证授权异常}：Spring Security 抛出的 \texttt{AuthenticationException} 和 \texttt{AccessDeniedException} 统一转换为 401 和 403 响应；

  \item \textbf{系统异常}：未预期的系统异常（如 \texttt{NullPointerException}、\texttt{SQLException}）统一返回 500 错误，隐藏内部实现细节，避免信息泄露；

  \item \textbf{异常日志}：所有异常均记录完整堆栈信息到日志文件，便于问题排查和监控告警。
\end{enumerate}

\subsection{日志分级策略}

系统采用 Logback 作为日志框架，日志分级策略如下：

\begin{enumerate}
  \item \textbf{ERROR 级别}：记录系统错误和异常，如数据库连接失败、外部服务调用失败、未捕获的运行时异常等，需要立即处理；

  \item \textbf{WARN 级别}：记录潜在问题和警告信息，如缓存未命中、降级策略触发、配置项缺失使用默认值等，需要关注但不影响核心功能；

  \item \textbf{INFO 级别}：记录关键业务操作，如用户登录、社团创建、活动报名、审批操作等，用于业务审计和问题追溯；

  \item \textbf{DEBUG 级别}：记录详细的调试信息，如 SQL 语句、方法参数、中间结果等，仅在开发和测试环境启用；

  \item \textbf{TRACE 级别}：记录最详细的跟踪信息，如框架内部调用链路，通常不启用，仅在深度排查问题时临时开启。
\end{enumerate}


\subsection{日志文件管理}

系统日志文件按日期滚动，保留策略如下：

\begin{enumerate}
  \item \textbf{应用日志}：\texttt{application.log}，记录应用运行日志，每天滚动一次，保留最近 30 天；

  \item \textbf{错误日志}：\texttt{error.log}，仅记录 ERROR 级别日志，每天滚动一次，保留最近 90 天；

  \item \textbf{访问日志}：\texttt{access.log}，记录所有 HTTP 请求，每天滚动一次，保留最近 7 天；

  \item \textbf{审计日志}：审计日志存储在数据库中，不设置自动清理策略，长期保留用于合规审计；

  \item \textbf{日志压缩}：滚动后的日志文件自动压缩为 gzip 格式，节省存储空间。
\end{enumerate}

\section{监控与告警设计}

\subsection{指标采集设计}

系统通过 Spring Boot Actuator 和 Micrometer 暴露监控指标，Prometheus 定时抓取：

\begin{enumerate}
  \item \textbf{JVM 指标}：堆内存使用率、GC 次数和耗时、线程数、类加载数等，监控应用运行状态；

  \item \textbf{HTTP 指标}：请求总数、响应时间分布（P50/P95/P99）、错误率等，监控接口性能；

  \item \textbf{数据库指标}：连接池使用率、SQL 执行时间、慢查询数量等，监控数据库性能；

  \item \textbf{缓存指标}：Redis 命中率、连接数、内存使用率等，监控缓存效果；

  \item \textbf{业务指标}：登录成功/失败次数、活跃社团数、待审批数量等，监控业务运行情况。
\end{enumerate}


\subsection{告警规则设计}

系统在 Prometheus 中配置告警规则，当指标异常时触发告警：

\begin{enumerate}
  \item \textbf{服务可用性告警}：应用实例下线超过 1 分钟，触发紧急告警；

  \item \textbf{错误率告警}：5 分钟内 HTTP 5xx 错误率超过 5\%，触发警告告警；

  \item \textbf{响应时间告警}：接口 P95 响应时间超过 2 秒，触发警告告警；

  \item \textbf{资源使用告警}：JVM 堆内存使用率超过 85\%，触发警告告警；超过 95\% 触发紧急告警；

  \item \textbf{数据库连接告警}：数据库连接池使用率超过 80\%，触发警告告警。
\end{enumerate}

\subsection{可视化监控面板}

系统使用 Grafana 构建可视化监控面板，主要包括：

\begin{enumerate}
  \item \textbf{系统概览面板}：展示应用实例状态、请求 QPS、错误率、响应时间等核心指标；

  \item \textbf{JVM 监控面板}：展示堆内存使用趋势、GC 频率、线程数变化等 JVM 运行状态；

  \item \textbf{数据库监控面板}：展示连接池使用率、SQL 执行时间分布、慢查询列表等数据库性能指标；

  \item \textbf{业务监控面板}：展示用户活跃度、社团数量趋势、活动报名统计等业务运营数据；

  \item \textbf{告警历史面板}：展示历史告警记录、告警频率统计、告警处理时长等告警管理数据。
\end{enumerate}


\section{本章小结}

本章详细阐述了校园社团管理与招新系统的设计方案，主要包括以下内容：

首先，在总体架构设计方面，系统采用模块化单体架构，将业务按领域划分为 8 个模块，既保持了单体应用的部署简便性，又具备清晰的模块边界。部署拓扑采用容器化方案，通过 Docker Compose 编排前端、后端、AI 服务、数据库、缓存、消息队列、监控等多个服务容器，实现一键部署。

其次，在数据模型设计方面，系统遵循第三范式设计了 19 张数据表，其中 18 张承担核心业务建模，1 张用于社团标签辅助描述，涵盖用户管理、社团管理、活动管理、招新管理、通知管理、资源管理、财务管理、审计管理等业务领域。通过合理的索引设计和唯一约束，保证了查询性能和数据完整性。

第三，在安全与权限设计方面，系统采用 JWT + HttpOnly Cookie + Redis 会话管理的混合认证方案，结合 Spring Security 的角色授权和业务权限服务，实现了细粒度的权限控制。认证时序设计清晰，支持令牌刷新和主动失效。

第四，在接口与前端交互设计方面，系统定义了统一的响应结构，前端通过 Pinia 管理认证状态，路由守卫实现访问控制，Axios 拦截器统一处理错误响应，保证了前后端交互的一致性。

第五，在 AI 服务设计方面，系统采用独立部署的微服务架构，AI 服务与主应用解耦。当前版本的知识库维护以启动初始化和手动补充为主，问答链路采用“业务工具优先 + RAG 补充 + 失败降级”的组合策略，推荐链路融合协同过滤和内容相似度算法。

第六，在性能优化设计方面，系统从数据库查询优化、缓存策略、并发控制三个维度进行优化。数据库层面通过索引优化、分页查询、延迟加载等策略提升查询性能；缓存层面使用 Redis 缓存会话、限流计数、热点数据；并发控制层面采用悲观锁、唯一约束、幂等性设计等机制保证数据一致性。

第七，在异常处理与日志设计方面，系统通过全局异常处理器统一处理各类异常，采用 Logback 实现日志分级和滚动管理，保证了系统的可维护性和可追溯性。

最后，在监控与告警设计方面，系统通过 Prometheus 采集 JVM、HTTP、数据库、缓存、业务等多维度指标，配置告警规则实现异常自动告警，使用 Grafana 构建可视化监控面板，为系统运维提供全面的监控支持。

通过以上设计，系统在架构合理性、数据模型完整性、安全性、性能、可维护性、可监控性等方面均达到了较高水平，为后续的系统实现奠定了坚实基础。
