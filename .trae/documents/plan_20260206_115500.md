I have analyzed the current codebase and the requirements. Here is the implementation plan to add the check-in code functionality without time restrictions.

## 1. Database Schema

* **File**: `community-gateway/src/main/resources/schema.sql`

* **Action**: Add `checkin_code` column to `t_activity` table.

## 2. Backend Implementation

### Entity & DTO

* **File**: `community-core/.../entity/Activity.java`

  * Add `private String checkinCode;` field.

* **File**: `community-core/.../dto/ActivityCreateDTO.java`

  * Add `private String checkinCode;` field to support setting it during creation.

### Service Layer

* **File**: `community-activity/.../service/ActivityService.java`

  * Update `signIn` method signature to accept `String code`.

* **File**: `community-activity/.../service/impl/ActivityServiceImpl.java`

  * Update `signIn` implementation.

  * **Logic**:

    * Verify user has signed up.

    * If activity has a `checkinCode`, verify the provided `code` matches.

    * **No time restrictions** (as requested).

    * Record attendance and update status.

### Controller Layer

* **File**: `community-activity/.../controller/ActivityController.java`

  * Update `POST /api/activities/{id}/signin` to accept a JSON body containing `code`.

## 3. Frontend Implementation

### Admin Side (Set Code)

* **File**: `frontend/src/views/admin/ActivityManagement.vue`

  * Add "Check-in Code" input field in the Activity Creation/Editing dialog.

  * Update table to optionally show the code (or keep it hidden/tooltip).

### Student Side (Perform Sign-in)

* **File**: `frontend/src/views/student/MyActivitiesView.vue`

  * Add a "Sign In" button for activities with `SIGNED` status.

  * Implement a dialog to input the check-in code.

  * Call the updated sign-in API.

