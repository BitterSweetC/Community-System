I will implement the missing frontend components for the Club Dissolution Lifecycle, matching the current backend implementation (which does not currently require verification codes).

### 1. Club Admin - Dissolution Application (`ClubSettings.vue`)
*   **Add "Danger Zone" Section**: Add a new card section at the bottom of the settings page.
*   **Dissolution Logic**:
    *   **If Active**: Show "Apply for Dissolution" button.
        *   Action: Open a dialog requiring a reason input.
        *   API: `POST /api/clubs/{id}/dissolve` (payload: `{ reason: "..." }`)
    *   **If Dissolving**: Show "Withdraw Application" button.
        *   Action: Confirm dialog.
        *   API: `POST /api/clubs/{id}/dissolve/withdraw`
    *   **If Dissolved**: Show status message (read-only).

### 2. System Admin - Management Actions (`ClubManagement.vue`)
*   **Status Display**: Update `getStatusType` to correctly render `DISSOLVING` (Orange/Warning) and `DISSOLVED` (Gray/Info) tags.
*   **New Actions**:
    *   **Force Dissolve**: Available for `ACTIVE` and `DISSOLVING` clubs.
        *   Action: Confirm dialog with optional reason.
        *   API: `DELETE /api/clubs/{id}/force` (query param: `reason`)
    *   **Restore Club**: Available for `DISSOLVED` clubs.
        *   Action: Confirm dialog.
        *   API: `POST /api/clubs/{id}/recover`

### 3. Implementation Details
*   Directly use `axios` in components to match existing pattern.
*   Add proper user feedback (success/error messages).
*   Refresh data after operations.
