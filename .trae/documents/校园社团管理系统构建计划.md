根据文档和现有代码分析，后端 (Spring Boot) 已有较完整的骨架代码，而前端 (Vue3) 仅处于初始化状态（空路由）。接下来的计划重点是**从零构建前端业务模块**并与后端进行对接。

### 📅 实施计划

#### 第一阶段：前端基础建设 (Frontend Infrastructure)
1.  **引入核心依赖**:
    *   安装 `axios` (API 请求)。
    *   安装 `element-plus` (UI 组件库，适合管理系统)。
    *   安装 `sass` (样式预处理)。
2.  **架构搭建**:
    *   配置全局拦截器 (`src/utils/request.ts`)：处理 JWT Token 注入和 401 过期跳转。
    *   建立 API 统一管理层 (`src/api/*.ts`)。
    *   配置 Vue Router 路由表及路由守卫 (Permission Guard)。
    *   配置 Pinia Store (`userStore`)：管理用户信息和登录状态。

#### 第二阶段：核心业务模块开发 (Core Modules)
1.  **认证模块 (Auth)**:
    *   实现 **登录页** (Login): 对接 `/api/auth/login`。
    *   实现 **注册页** (Register): 对接 `/api/auth/register`。
2.  **公共区域 (Public)**:
    *   **社团广场** (Club List): 展示社团卡片，支持搜索/分类筛选。
    *   **社团详情** (Club Detail): 展示简介、公告、招新入口。
3.  **学生中心 (Student Dashboard)**:
    *   **我的社团**: 查看已加入社团。
    *   **我的申请**: 查看招新/活动申请进度。
4.  **社团管理后台 (Club Admin)**:
    *   **招新管理**: 创建招新批次、审批申请 (Recruit Review)。
    *   **成员管理**: 成员列表、角色变更。
    *   **活动发布**: 创建活动、生成签到码。

#### 第三阶段：后端微调与联调 (Backend & Integration)
1.  **CORS 与安全**: 确认 `SecurityConfig` 允许前端跨域（已检查，配置正确）。
2.  **数据初始化**: 如果数据库为空，提供简单的 SQL 脚本或通过 API 创建初始管理员账号。
3.  **接口对接**: 联调社团列表、登录、申请提交等核心流程。

### 🛠️ 准备工作确认
*   **数据库**: 请确保本地 MySQL 已启动，并创建数据库 `community_db`。
*   **Redis**: 请确保本地 Redis 已启动 (端口 6379)。

确认计划后，我将从前端基础建设开始执行。