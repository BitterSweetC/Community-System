为了确保项目能够“正确运行”并进行功能测试，我们需要解决以下关键问题：

1. **缺失初始数据**：目前数据库是空的，没有账号无法登录，也没有社团数据可供展示。
2. **服务未启动**：后端和前端服务均未运行。

因此，我的执行计划如下：

### 1. 后端增强：自动注入测试数据

创建一个数据初始化组件 `DataInitializer`，在系统启动时自动检查并填充以下数据（如果不存在）：

* **基础角色**：`ROLE_USER`, `ROLE_ADMIN`, `ROLE_CLUB_ADMIN`。

* **管理员账号**：用户名 `admin`，密码 `123456`。

* **测试社团**：创建几个示例社团（如“吉他社”、“编程俱乐部”），以便前端展示。

### 2. 环境验证与服务启动

* **后端启动**：使用 Maven 启动 Spring Boot 应用。这将自动连接本地 MySQL (community\_db) 和 Redis。

* **前端启动**：使用 npm 启动 Vue 开发服务器。

### 3. 验证与交付

* 确保后端日志无报错，数据库表自动创建。

* 确保前端可访问，并尝试模拟登录流程验证系统联通性。

* 最终提供访问地址和默认账号密码给您。

**注意**：此计划假设您的本地环境已安装并运行了 **MySQL (端口3306, 账号root/gyh#, 库community\_db)** 和 **Redis (端口6379)**。如果尚未安装，请告知，否则服务将无法启动。
